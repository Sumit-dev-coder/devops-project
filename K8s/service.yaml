apiVersion: v1
kind: Service
metadata:
  name: devops-project-service
  labels:
    app: devops-project
spec:
  type: NodePort
  selector:
    app: devops-project
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080